{
  "version": 2,
  "buildCommand": null,
  "outputDirectory": ".",
  "installCommand": null,
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/server.py" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "functions": {
    "/api/server.py": {
      "runtime": "python3.11",
      "maxDuration": 60
    }
  },
  "env": {
    "FLASK_ENV": "production"
  }
}